# L(X, sep) — a list with an explicit separator — generates and parses.
#
# Found while building the test harness.

@lexer
tokens
    ident
    semi
    lbracket
    rbracket
    eof
    err
';'                                         → semi
'['                                         → lbracket
']'                                         → rbracket
[a-zA-Z_][a-zA-Z0-9_]*                      → ident
.                                           → err

@parser
@lang = "list_separator"

top! = "[" L(item, ";") "]"                 → (p ...2)
item = IDENT                                → 1
