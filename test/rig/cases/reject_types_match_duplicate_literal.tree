(module
  (sub
    `main`
    ()
    _
    (block
      (set _ `n` _ `2`)
      (match
        `n`
        (arm (range_pattern `1` `3`) (call `print` `"low"`))
        (arm `2` (call `print` `"two"`))
        (arm `else` (call `print` `"other"`))))))
