# The MUMPS grammar generated with --spans: node spans next to src ids (the
# lexer's dot-level attribute) on a legacy grammar with many untagged lists.
grammar=../mumps/mumps.grammar
lang=../mumps/mumps.zig
flags=--spans
