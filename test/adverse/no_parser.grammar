# A grammar file needs an @parser section (an empty one generates a lexer).
# error: no_parser.grammar:11:1:
# error: no @parser section
@lexer
tokens
    ident
    eof
    err
[a-zA-Z]+ → ident
. → err
