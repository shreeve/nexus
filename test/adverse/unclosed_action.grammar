# Lexer action missing closing brace
# error: unclosed_action.grammar:9:
@lexer
tokens
    ident
    newline
    eof
    err
'\n' → newline, {beg = 1
[a-zA-Z]+ → ident
. → err
@parser
# (no @conflicts block: the grammar must be conflict-free)
program! = IDENT → (module 1)
