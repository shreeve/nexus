# Lexer rule missing arrow
# error: no_arrow.grammar:8:
@lexer
tokens
    ident
    eof
    err
[a-zA-Z]+ ident
. → err
@parser
# (no @conflicts block: the grammar must be conflict-free)
program! = IDENT → (module 1)
