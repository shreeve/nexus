# The self-hosted frontend: nexus.grammar describes a whole .grammar file.
# Cases are grammar-file text: a whole file (lexer.txt), or @parser-section
# text alone.
grammar=../../nexus.grammar
lang=$(ls ../../src/frontend/lang.zig ../../src/lang.zig 2>/dev/null | head -1)
