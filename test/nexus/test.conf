# The self-hosted frontend: nexus.grammar describes the @parser section of a
# .grammar file. Cases are parser-section text (what follows `@parser`).
grammar=../../nexus.grammar
lang=$(ls ../../src/frontend/lang.zig ../../src/lang.zig 2>/dev/null | head -1)
